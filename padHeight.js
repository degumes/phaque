const size = 1.0 / 4.0

const padHeight = new Float32Array([
  size,
  size,
  size,
  size,
  size,
  size,
  size,
  size,
  size,
  size,
  size,
  size,
  size,
  size,
  size
])

export default padHeight
